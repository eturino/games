# import Partial from './partial.graphql'
# import Round from './round.graphql'

fragment Game on Game {
  id
  lastRoundNumber
  name
  phase
  status
  round {
    ...Round
  }
  players {
    ...Partial
  }
}
